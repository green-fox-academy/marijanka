var numbers = [7, 8, -1, 4, 3, 12];
var min = numbers[0];

for (var i = 1; i < numbers.length; i++) {
  if (numbers[i] < min) {
    min = numbers[i]
  }
}
console.log(min)

for (var i = 1; i < numbers.length; i++) {
  minimum = (numbers[i] < minimum) ? minimum = numbers[i]
}
